# Spatiotemporal Runtime Lifecycle Test

Temporary controlled fixture used to verify GitHub discovery, audit, approval, staging, activation, runtime health, and automatic rollback.

The repository contains no install scripts or dependencies and is deleted after the acceptance test.
